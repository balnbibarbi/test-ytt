#@ load("transforms.star", "transforms")
#@ load("@ytt:overlay", "overlay")
#@ def do_transform(input, _):
#@   # print("Input:")
#@   # transforms.dump(input)
#@   input = transforms.to_primitive(input)
#@   (index, object) = transforms.select(input, colour="red", shape="circle")
#@   # transforms.dump(index, object)
#@   return object
#@ end
#@overlay/match by=overlay.all, expects="1+"
#@overlay/replace via=do_transform
---
